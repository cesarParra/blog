---
import {RHeader} from './RHeader.tsx';

const currentPath = Astro.url.pathname;
const {isHomePage} = Astro.props;
---

<RHeader currentPath={currentPath} isHomePage={isHomePage} client:only="react"/>
